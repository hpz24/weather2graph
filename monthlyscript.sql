SELECT 
    CONCAT(SUBSTRING(TIME,1,13),":00:00")AS TIMEINTER, 
    ROUND(AVG(WERT),2) AS WERT
FROM `1press30`
    WHERE 
SUBSTRING(TIME,1,7) = '2015-07'
    GROUP BY SUBSTRING(TIME,1,13)
INTO OUTFILE '/tmp/2015-07-17-press.csv'
	FIELDS TERMINATED BY ','
	LINES TERMINATED BY '\n';
